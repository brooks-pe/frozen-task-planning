
  # SyncPoint Prototype

  This is a code bundle for SyncPoint Prototype. The original project is available at https://www.figma.com/design/5NF2DhRMxII1bxVBQFJwrc/SyncPoint-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  